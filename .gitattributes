# Scripts shell et hook git : toujours LF (sinon core.autocrlf=true les convertit
# en CRLF au checkout et bash echoue avec "\r: command not found").
*.sh text eol=lf
LLM-wiki-Assessment/eval/hooks/* text eol=lf
