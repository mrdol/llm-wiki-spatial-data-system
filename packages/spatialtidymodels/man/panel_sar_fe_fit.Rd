% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/70-panel-spatial.R
\name{panel_sar_fe_fit}
\alias{panel_sar_fe_fit}
\title{Fit a spatial-lag (SAR) fixed/random-effects panel model}
\usage{
panel_sar_fe_fit(formula, data, panel, W, model_type = c("within", "random"))
}
\arguments{
\item{formula}{A model formula, e.g. \code{log(gsp) ~ log(pcap) + log(pc)}.}

\item{data}{A data frame with one row per unit-time observation.}

\item{panel}{A \code{spatial_panel_spec} (see \code{\link[=spatial_panel_spec]{spatial_panel_spec()}}).}

\item{W}{A spatial weights matrix defined over units (see
\code{\link[=align_panel_W]{align_panel_W()}}) -- not a \code{listw} object; converted internally with
\code{spdep::mat2listw()}.}

\item{model_type}{One of \code{"within"} (fixed effects) or \code{"random"}.}
}
\value{
A \code{spatial_panel_fit} wrapping the \code{splm} object, with
\code{lag_coefficient} in \code{extra}.
}
\description{
Thin wrapper around \code{splm::spml(..., lag = TRUE, spatial.error = "none")}.
Distinct from the cross-sectional \code{sar_lag} estimator: the spatial
dependence, effects structure, and standard errors are all panel-specific
(Elhorst 2003; Millo & Piras 2012, JSS 47).
}
