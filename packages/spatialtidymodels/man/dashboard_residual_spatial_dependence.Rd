% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_residual_spatial_dependence}
\alias{dashboard_residual_spatial_dependence}
\title{Residual spatial dependence, corrected for E(I) under CSR when appropriate}
\usage{
dashboard_residual_spatial_dependence(suite, cv_scheme = NULL)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} or a results-shaped \code{data.frame}.}

\item{cv_scheme}{\code{NULL} (default) requires \code{suite} to already cover a
single CV scheme; pass a scheme name to filter to it explicitly.}
}
\value{
\code{results} (after the \code{cv_scheme} filter) with two columns added:
\code{residual_spatial_dependence} (the corrected or fallback value, \code{NA} if
neither \code{moran_i} nor \code{moran_abs} is available) and
\code{residual_spatial_dependence_method} (\code{"corrected"},
\code{"moran_abs_fallback"}, or \code{NA}).
}
\description{
\code{moran_abs} (\verb{|I|}) treats a small positive \code{I} on a small test sample the
same as a small positive \code{I} on a huge one, but under complete spatial
randomness \code{E(I) = -1/(n-1)} is not \code{0} -- for small \code{n}, a mildly
negative \code{I} can be \emph{closer} to what pure randomness predicts than a
mildly positive one. \verb{|I - E(I)|} corrects for this, but it is only
statistically meaningful when the row's own \code{n} is the exact test size
the row's \code{moran_i} was computed on.
}
\details{
At the aggregated-row level (one row per dataset x estimator x cv_scheme),
\code{moran_i} is the \strong{unweighted mean} of each fold/repetition's own Moran's
I (see \code{summarize_resample_results()} in R/13), while \code{n} is the
\strong{pooled} test size across all those folds. For any cv_scheme with more
than one evaluation (\code{n_resamples > 1}, e.g. \code{near_prediction}'s
repetitions or \code{block_spatial}'s folds), pairing the pooled \code{n} with the
averaged \code{I} in \code{E(I) = -1/(n-1)} would not correspond to any single
Moran test actually run -- silently misleading. The correction is
therefore applied only where \code{n_resamples == 1} (a single evaluation,
e.g. \code{holdout_10pct} or \code{in_sample}); every other row falls back to
\code{moran_abs}.
}
