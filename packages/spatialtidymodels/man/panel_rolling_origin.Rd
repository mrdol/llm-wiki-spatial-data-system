% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/70-panel-spatial.R
\name{panel_rolling_origin}
\alias{panel_rolling_origin}
\title{Build growing-window rolling-origin folds over a panel's periods}
\usage{
panel_rolling_origin(data, panel, initial, assess, skip = 0L)
}
\arguments{
\item{data}{A data frame with one row per unit-time observation.}

\item{panel}{A \code{spatial_panel_spec}.}

\item{initial}{Number of periods in the first training window.}

\item{assess}{Number of periods held out for testing in each fold.}

\item{skip}{Number of periods to skip between the end of one assessment
window and the start of the next training window (default \code{0}, i.e. the
next fold's test window starts immediately after the previous one).}
}
\value{
A list of \code{panel_time_split} objects, one per fold, in
chronological order.
}
\description{
Each fold's training window grows from the first \code{initial} periods;
\code{assess} subsequent periods are held out for testing, and the origin
advances by \code{assess + skip} periods between folds. Mirrors
\code{rsample::rolling_origin()}'s semantics, but cut on the panel's ordered
unique periods rather than on rows (a panel row is a unit-time pair, not
an independent observation).
}
