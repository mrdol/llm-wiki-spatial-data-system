% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/70-panel-spatial.R
\name{align_panel_W}
\alias{align_panel_W}
\title{Align a spatial weights matrix to the canonical (sorted) unit order}
\usage{
align_panel_W(units, W, w_unit_order = NULL)
}
\arguments{
\item{units}{Unit identifiers actually present in the panel data (any
order, duplicates allowed -- only the unique set matters).}

\item{W}{A square matrix (or an object coercible to one) of spatial
weights defined over units.}

\item{w_unit_order}{Optional vector giving the unit identifiers in \code{W}'s
row/column order, required when \code{W} has no dimnames.}
}
\value{
\code{W} as a matrix, reordered to \code{sort(unique(units))}.
}
\description{
Guarantees that the fit is invariant to the row order of \code{data} and to the
row/column order of \code{W}: both are always reordered to \code{sort(unique(units))}
before being handed to a panel engine.
}
