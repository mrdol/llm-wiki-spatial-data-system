% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_metric_by_estimator}
\alias{dashboard_metric_by_estimator}
\title{Aggregate a metric per estimator across all filtered rows}
\usage{
dashboard_metric_by_estimator(
  suite,
  metric,
  fun = stats::median,
  cv_scheme = NULL
)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} or a results-shaped \code{data.frame}.}

\item{metric}{Column to aggregate, e.g. \code{"moran_i"}.}

\item{fun}{Aggregation function applied to the finite values of \code{metric}
for each estimator. Default \code{stats::median}.}

\item{cv_scheme}{\code{NULL} (default) requires \code{suite} to already cover a
single CV scheme; pass a scheme name to filter to it explicitly. Errors
rather than silently pooling multiple schemes together.}
}
\value{
A \code{data.frame} with \code{estimator}, \code{value}. An estimator with no
finite values gets \code{NA} rather than being dropped.
}
\description{
Generic building block for a single-metric summary chart (e.g. residual
spatial autocorrelation): one aggregated value per estimator, computed
only over finite values. Deliberately does not collapse cv_scheme first --
pass an already cv_scheme-filtered \code{suite}/\code{results} if that distinction
matters, same convention as the rest of this file.
}
