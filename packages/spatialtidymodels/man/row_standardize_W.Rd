% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/70-panel-spatial.R
\name{row_standardize_W}
\alias{row_standardize_W}
\title{Row-standardize a unit-level spatial weights matrix}
\usage{
row_standardize_W(W)
}
\arguments{
\item{W}{A square, aligned spatial weights matrix.}
}
\value{
\code{W} with each row divided by its row sum.
}
\description{
Every panel spatial engine in this module calls \code{spdep::mat2listw(W, style = "W")} to fit (row-standardized weights), so \code{W} as stored in
\code{spatial_panel_fit$extra} and as used by \code{\link[=build_panel_wx]{build_panel_wx()}} and
\code{\link[=panel_sar_sdm_impacts]{panel_sar_sdm_impacts()}} must be row-standardized too -- otherwise the
impacts decomposition and WX construction would silently use a different W
convention than the one the model was actually estimated with. This only
matters when the caller's \code{W} is not already row-standardized (e.g. a raw
0/1 contiguity matrix); a pre-standardized \code{W} is unaffected (row sums
already equal to 1).
}
