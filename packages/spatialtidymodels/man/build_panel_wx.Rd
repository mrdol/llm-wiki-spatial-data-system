% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/70-panel-spatial.R
\name{build_panel_wx}
\alias{build_panel_wx}
\title{Build spatially-lagged covariates (WX) for a panel Spatial Durbin Model}
\usage{
build_panel_wx(data, panel, W, x_formula)
}
\arguments{
\item{data}{A data frame with one row per unit-time observation.}

\item{panel}{A \code{spatial_panel_spec}.}

\item{W}{A spatial weights matrix defined over units.}

\item{x_formula}{A one-sided formula naming the covariates to lag (the
fixed-effect model's RHS; the intercept, if any, is dropped).}
}
\value{
A data frame with one \verb{W_<term>} column per RHS term of
\code{x_formula}, aligned row-for-row with \code{data}. Requires a balanced panel
(one row per unit for every period).
}
\description{
\code{splm} does not offer a native Durbin ("no option is inferred from its
name" per the implementation plan) -- WX is built explicitly here, one
time period at a time, and verified independently in
\code{tests/testthat/test-panel-spatial.R} against a hand-written per-period
loop using a different code path.
}
