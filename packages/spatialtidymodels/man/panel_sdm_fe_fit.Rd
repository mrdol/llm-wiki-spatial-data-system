% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/70-panel-spatial.R
\name{panel_sdm_fe_fit}
\alias{panel_sdm_fe_fit}
\title{Fit a panel Spatial Durbin Model (SDM): spatial lag plus WX covariates}
\usage{
panel_sdm_fe_fit(formula, data, panel, W, model_type = c("within", "random"))
}
\arguments{
\item{formula}{A model formula, e.g. \code{log(gsp) ~ log(pcap) + log(pc)}.}

\item{data}{A data frame with one row per unit-time observation.}

\item{panel}{A \code{spatial_panel_spec} (see \code{\link[=spatial_panel_spec]{spatial_panel_spec()}}).}

\item{W}{A spatial weights matrix defined over units (see
\code{\link[=align_panel_W]{align_panel_W()}}) -- not a \code{listw} object; converted internally with
\code{spdep::mat2listw()}.}

\item{model_type}{One of \code{"within"} (fixed effects) or \code{"random"}.}
}
\value{
A \code{spatial_panel_fit} with \code{lag_coefficient} and the WX term names
(\code{wx_terms}) in \code{extra}.
}
\description{
Augments \code{formula} with spatially-lagged covariates (see
\code{\link[=build_panel_wx]{build_panel_wx()}}) and fits the result with
\code{splm::spml(..., lag = TRUE, spatial.error = "none")}. This is a lag model
on an augmented regressor set, not a native "Durbin" backend option.
}
