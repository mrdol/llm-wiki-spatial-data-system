% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/16-estimator-comparison.R
\name{compare_estimator_variant}
\alias{compare_estimator_variant}
\title{Compare a candidate estimator to a reference estimator across datasets}
\usage{
compare_estimator_variant(
  suite,
  reference,
  candidate,
  cv_scheme = NULL,
  primary_metric = "rmse",
  secondary_metrics = c("mae", "moran_abs", "duration_sec"),
  lower_is_better = c(rmse = TRUE, mae = TRUE, moran_abs = TRUE, duration_sec = TRUE),
  rules = comparison_rules(),
  wilcoxon = TRUE,
  groups = NULL,
  dataset_metadata = NULL
)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} (from \code{\link[=benchmark_spatial_suite]{benchmark_spatial_suite()}})
or a results-shaped \code{data.frame} with at least \code{dataset}, \code{cv_scheme},
\code{estimator} and the metric columns referenced by \code{primary_metric}/
\code{secondary_metrics}.}

\item{reference, candidate}{Estimator names present in the results table
(see \code{available_benchmark_estimators()}/the \code{estimator} column).}

\item{cv_scheme}{\code{NULL} (default) to auto-detect: a single scheme present
is compared directly, multiple schemes are compared independently and
returned as an \code{estimator_comparison_by_scheme}. Pass a single scheme
name to restrict to it, or a character vector of schemes to force the
by-scheme comparison over exactly those.}

\item{primary_metric}{Metric that drives the win/tie/loss classification
and the verdict. Default \code{"rmse"}.}

\item{secondary_metrics}{Additional metrics to report deltas for. Any
metric named in \code{rules$secondary_guardrails} is added automatically even
if omitted here, since its delta must exist for the guardrail to apply.
Default \code{c("mae", "moran_abs", "duration_sec")}.}

\item{lower_is_better}{Named logical vector giving the direction of each
metric referenced above (\code{TRUE} = lower is better). Metrics not listed
default to \code{TRUE}.}

\item{rules}{A \code{\link[=comparison_rules]{comparison_rules()}} object. Default \code{comparison_rules()}.}

\item{wilcoxon}{If \code{TRUE} (default), run a paired Wilcoxon signed-rank test
on the primary metric's per-case delta and require significance (at
\code{rules$alpha}) for a \code{"SUPERIOR"}/\code{"INFERIOR"} verdict. Set \code{FALSE} to
fall back to pure threshold counting.}

\item{groups}{Optional subgroup analysis: a \code{data.frame} with a \code{dataset}
column and exactly one grouping column (e.g. an N bucket, a domain
label, a geometry type -- anything you supply, this function does not
compute meta-features itself). When the candidate is not globally
\code{"SUPERIOR"}/\code{"INFERIOR"} (i.e. the verdict would otherwise be
\code{"EQUIVALENT"} or \code{"INCONCLUSIVE"}), each group's own win rate is
checked against \code{rules$min_win_rate}/\code{rules$min_cases_for_subgroup}; if
any group qualifies, the verdict becomes \code{"SPECIALIZED"} instead --
"not better everywhere, but systematically better on this identifiable
subset". The full per-group breakdown is always returned in
\verb{$subgroups$table}, whether or not it changed the verdict. \code{NULL}
(default) skips subgroup analysis entirely -- existing calls are
unaffected. Not supported when \code{rules$analysis_unit == "source"} (errors).}

\item{dataset_metadata}{Only used when \code{rules$analysis_unit == "source"}:
a \code{data.frame} with \code{dataset} and \code{source_dataset_id} columns, giving the
mapping used to collapse tasks into sources. \code{NULL} (default) auto-fills
from \code{suite$dataset_metadata} when \code{suite} is a \code{spatial_benchmark_suite};
required explicitly when \code{suite} is a plain results \code{data.frame}.}
}
\value{
An \code{estimator_comparison} object (or \code{estimator_comparison_by_scheme}
when multiple CV schemes are in play) with \code{per_case} (one row per case),
\code{summary} (aggregate statistics, including the CV scheme it covers),
\code{guardrails} (secondary-guardrail breaches and their values), \code{subgroups}
(per-group breakdown when \code{groups} was supplied, else \code{NULL}), \code{wilcoxon}
(the \code{htest} result, or \code{NULL}), \code{rules}, \code{verdict} and
\code{verdict_reasons} (why that verdict, not just what it is).
}
\description{
Computes per-dataset (per \code{dataset} x \code{cv_scheme} "case") relative deltas
between a candidate and a reference estimator on a primary metric and any
number of secondary metrics, classifies each case as a win, tie or loss
against a region of practical equivalence, and turns the aggregate picture
into a verdict using \code{\link[=comparison_rules]{comparison_rules()}} plus (by default) a paired
Wilcoxon signed-rank test on the primary metric's deltas.
}
\details{
A "case" is a (dataset, cv_scheme) pair. The same dataset evaluated under
several CV schemes produces several cases that are \strong{not} independent
data sources, so this function never pools them into one statistical test
silently. If \code{results} contains more than one CV scheme for the pair of
estimators being compared and \code{cv_scheme} is left \code{NULL}, it returns an
\code{estimator_comparison_by_scheme} object -- a named list with one
independent \code{estimator_comparison} per scheme -- instead of a single
comparison mixing schemes. Pass \code{cv_scheme} (a single scheme, or a
character vector of schemes) to control this explicitly; with exactly one
scheme to process, a plain \code{estimator_comparison} is returned.

This function does no plotting. A dashboard should call it and display
\code{cmp$verdict}, \code{cmp$summary} and \code{cmp$per_case} -- it should never
recompute or override the verdict logic.
}
