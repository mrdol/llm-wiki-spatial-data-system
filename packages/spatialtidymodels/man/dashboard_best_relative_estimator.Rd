% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_best_relative_estimator}
\alias{dashboard_best_relative_estimator}
\title{Pick the best estimator by median relative metric, for a KPI card}
\usage{
dashboard_best_relative_estimator(
  suite,
  baseline_estimator,
  metric = "rmse",
  lower_is_better = TRUE,
  cv_scheme = NULL
)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} or a results-shaped \code{data.frame}.}

\item{baseline_estimator}{Estimator every other estimator is expressed
relative to, e.g. \code{"ols"}.}

\item{metric}{Metric to compute the ratio on. Default \code{"rmse"}.}

\item{lower_is_better}{Whether lower values of \code{metric} are better.
Default \code{TRUE}.}

\item{cv_scheme}{\code{NULL} (default) requires \code{suite} to already cover a
single CV scheme; pass a scheme name to filter to it explicitly. Errors
rather than silently pooling multiple schemes together.}
}
\value{
A list: \code{metric}, \code{estimator} (\code{NA} if no estimator has a finite
value), \code{value} (the median relative ratio), \code{baseline}, \code{n_datasets}.
}
\description{
Thin wrapper around \code{\link[=dashboard_relative_metric_by_estimator]{dashboard_relative_metric_by_estimator()}} that picks
the single best row -- e.g. "Best median relative RMSE: 0.72x, SpBoost SAR
ML, vs SAR Lag". Never picks based on a raw, cross-dataset-incomparable
metric value (see \code{\link[=dashboard_relative_metric_by_estimator]{dashboard_relative_metric_by_estimator()}}).
}
