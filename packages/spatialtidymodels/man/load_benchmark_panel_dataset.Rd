% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/72-panel-dataset-registry.R
\name{load_benchmark_panel_dataset}
\alias{load_benchmark_panel_dataset}
\title{Load a registered spatial panel dataset (data, spec, W, formula)}
\usage{
load_benchmark_panel_dataset(dataset, data_dir = NULL)
}
\arguments{
\item{dataset}{Dataset id, as listed by \code{\link[=available_panel_datasets]{available_panel_datasets()}}.}

\item{data_dir}{Optional override for resolving relative paths (RDS, W)
when not run from the repository root.}
}
\value{
A list with \code{data} (geometry dropped), \code{panel} (a
\code{spatial_panel_spec}), \code{W} (matrix, or \code{NULL} if no \code{w_file} is
declared), \code{formula}, and \code{spec} (the raw registry row).
}
\description{
Load a registered spatial panel dataset (data, spec, W, formula)
}
