% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_relative_metric_by_scheme}
\alias{dashboard_relative_metric_by_scheme}
\title{Median relative metric per estimator, computed independently per CV scheme}
\usage{
dashboard_relative_metric_by_scheme(
  suite,
  baseline_estimator,
  metric = "rmse",
  lower_is_better = TRUE,
  cv_schemes = NULL
)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} or a results-shaped \code{data.frame}.}

\item{baseline_estimator}{Estimator every other estimator is expressed
relative to, e.g. \code{"ols"}.}

\item{metric}{Metric to compute the ratio on. Default \code{"rmse"}.}

\item{lower_is_better}{Whether lower values of \code{metric} are better.
Default \code{TRUE}.}

\item{cv_schemes}{CV schemes to include. \code{NULL} (default) uses every
scheme present in \code{suite}.}
}
\value{
A \code{data.frame}: \code{cv_scheme}, \code{estimator}, \code{median_relative},
\code{n_datasets}. A scheme with no valid cases for \code{baseline_estimator}
contributes no rows rather than erroring.
}
\description{
Runs \code{\link[=dashboard_relative_metric_by_estimator]{dashboard_relative_metric_by_estimator()}} once per CV scheme and
stacks the results with a \code{cv_scheme} column -- this is the building
block for a CV Schemes comparison page: it must be possible to see that
an estimator does well in \code{near_prediction} but poorly in
\code{block_spatial}, which requires the per-scheme numbers to stay separate.
Never pools cases across schemes into one number (the way
\code{\link[=dashboard_relative_metric]{dashboard_relative_metric()}} would if called on unfiltered multi-scheme
data without \verb{cv_scheme=}).
}
