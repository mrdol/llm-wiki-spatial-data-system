% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/51-parsnip-inlaspde.R
\name{inla_spde_default_priors}
\alias{inla_spde_default_priors}
\title{Priors PC par defaut pour le champ SPDE, derives des donnees}
\usage{
inla_spde_default_priors(
  coords,
  y,
  family = "gaussian",
  prior_range = NULL,
  prior_sigma = NULL
)
}
\description{
Regle deterministe et reproductible (pas de reglage manuel par jeu de
donnees): \code{prior.range = c(diagonale_bbox / 5, 0.5)} -- forme PC-prior
standard ("weak default", 50\% de probabilite a priori de depassement,
Fuglstad et al. 2019). Pour \code{prior.sigma}, l'echelle pertinente depend du
lien: \code{family = "gaussian"} (lien identite) utilise \code{c(ecart_type(y), 0.5)}
-- l'amplitude du champ est directement sur l'echelle de la reponse. Pour
les familles a lien non-identite (\code{"binomial"}/\code{"poisson"}), l'ecart-type
de Y brut n'a pas de sens sur l'echelle du predicteur lineaire (ex. Y
binaire 0/1 a un ecart-type ~0.5, sans rapport avec l'amplitude
raisonnable d'un effet spatial en logit/log) -- \code{c(1, 0.5)} est utilise a
la place, aligne sur le prior faiblement informatif de Loca et al. (2025,
\code{paper_flapper_skate_presence}), qui utilisent un ecart-type ~1.4 (teste
jusqu'a 0.5) pour leurs effets fixes en contexte similaire. Les valeurs
passees explicitement dans \code{prior_range}/\code{prior_sigma} court-circuitent le
calcul correspondant.
}
\keyword{internal}
