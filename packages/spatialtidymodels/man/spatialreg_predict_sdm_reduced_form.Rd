% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10-parsnip-spatialreg.R
\name{spatialreg_predict_sdm_reduced_form}
\alias{spatialreg_predict_sdm_reduced_form}
\title{Forme reduite manuelle pour SDM (type="mixed") hors echantillon}
\usage{
spatialreg_predict_sdm_reduced_form(
  fit_obj,
  test_data,
  x_vars,
  coords,
  k_neighbors,
  durbin_vars = x_vars
)
}
\description{
SDM a besoin de W\emph{X pour les nouvelles observations en plus de W}y.
\code{predict.Sarlm(all.data=FALSE)} ne sait pas la construire hors echantillon
pour ce type -- confirme le 2026-07-06 sur london_hp: "Input data and
neighbourhood list have different dimensions", que ce soit newdata=test
seul (listw train+test) ou newdata=train+test complet (listw incoherente
avec les row.names internes du fit). On calcule donc
la forme reduite nous-memes sur un systeme ferme limite au fold de test:
W_test (kNN parmi les points de test uniquement), X_test avec ses lags
W_test \%\emph{\% X_test, puis y_hat = (I - rho}W_test)^-1 \%\emph{\% (design \%}\% beta).
C'est une approximation (le systeme "vrai" inclurait aussi les voisins
d'entrainement), mais elle produit un resultat defendable plutot qu'un
plantage ou -- pire -- un nombre faux silencieux.
}
\details{
NE PAS remplacer par un appel standard \code{stats::predict.Sarlm()} sans
revalider empiriquement: deux alternatives ont deja ete testees et ecartees
(2026-08/09). \verb{pred.type="TS", power=TRUE} ne plante plus (le bug de
dimension d'origine semble specifique a \code{power=FALSE}), mais reste
clairement moins precis que cette forme reduite maison -- RMSE 1,5 a 3x
pire, sur les 4 jeux testes (\code{ewhp}, \code{boston_housing}, \code{london_hp},
\code{georgia}), a chaque pli sans exception. \code{pred.type="KP2"} n'a pas ete
teste directement pour SDM mais herite du meme constat de principe. Garder
cette implementation tant qu'aucune alternative testee ne fait mieux.
}
\keyword{internal}
