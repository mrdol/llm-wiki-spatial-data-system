% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_n_tertile_groups}
\alias{dashboard_n_tertile_groups}
\title{Bucket datasets into dataset-size tertiles for subgroup analysis}
\usage{
dashboard_n_tertile_groups(dataset_metadata, min_datasets = 6L)
}
\arguments{
\item{dataset_metadata}{\code{suite$dataset_metadata} (or an equivalent
\code{data.frame} with \code{dataset} and \code{n} columns).}

\item{min_datasets}{Minimum number of datasets with a known \code{n} required
before bucketing is attempted. Default \code{6} (so each tertile has at
least ~2 datasets).}
}
\value{
A \code{data.frame(dataset, n_tertile)} with \code{n_tertile} in
\code{c("small", "medium", "large")} (fewer levels if the data doesn't span
enough distinct values for 3 buckets), or \code{NULL}.
}
\description{
Builds a \code{groups} data.frame usable directly as
\code{compare_estimator_variant(groups = ...)}'s subgroup dimension, from a
suite's \code{dataset_metadata$n} (see \code{\link[=benchmark_spatial_suite]{benchmark_spatial_suite()}}). Returns
\code{NULL} when there isn't enough real per-dataset \code{n} to make tertiles
meaningful -- this never invents a grouping that isn't backed by actual
dataset metadata; a page should render "Not enough metadata for subgroup
analysis" in that case rather than fabricating buckets.
}
