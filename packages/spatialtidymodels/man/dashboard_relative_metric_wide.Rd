% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_relative_metric_wide}
\alias{dashboard_relative_metric_wide}
\title{Pivot the estimator-vs-baseline relative metric into a heatmap-ready matrix}
\usage{
dashboard_relative_metric_wide(
  suite,
  baseline_estimator,
  metric = "rmse",
  lower_is_better = TRUE,
  cv_scheme = NULL,
  summary_row = c("median", "mean", "none")
)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} or a results-shaped \code{data.frame}.}

\item{baseline_estimator}{Estimator every other estimator is expressed
relative to, e.g. \code{"ols"}.}

\item{metric}{Metric to compute the ratio on. Default \code{"rmse"}.}

\item{lower_is_better}{Whether lower values of \code{metric} are better.
Default \code{TRUE}.}

\item{cv_scheme}{\code{NULL} (default) requires \code{suite} to already cover a
single CV scheme; pass a scheme name to filter to it explicitly. Errors
rather than silently pooling multiple schemes together.}

\item{summary_row}{Which cross-dataset summary to append as a final row:
\code{"median"} (default -- robust to one dataset having an extreme ratio,
preferred per project convention) or \code{"mean"}. Set \code{"none"} to omit it.}
}
\value{
A \code{data.frame} with a \code{dataset} column followed by one column per
estimator (baseline first), values are the \code{relative} ratio.
}
\description{
Wraps \code{\link[=dashboard_relative_metric]{dashboard_relative_metric()}} and reshapes its long-form output into
a wide \verb{dataset x estimator} table of \code{relative} values, with the baseline
column (always \code{1}) included and an appended summary row (column-wise,
ignoring \code{NA}) -- shaped for a color-graded HTML table rather than a
ggplot heatmap.
}
