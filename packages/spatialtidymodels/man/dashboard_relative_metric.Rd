% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_relative_metric}
\alias{dashboard_relative_metric}
\title{Compute a dataset x estimator metric table relative to a baseline}
\usage{
dashboard_relative_metric(
  suite,
  baseline_estimator,
  metric = "rmse",
  lower_is_better = TRUE,
  cv_scheme = NULL
)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} or a results-shaped \code{data.frame}.}

\item{baseline_estimator}{Estimator every other estimator is expressed
relative to, e.g. \code{"ols"}.}

\item{metric}{Metric to compute the ratio on. Default \code{"rmse"}.}

\item{lower_is_better}{Whether lower values of \code{metric} are better.
Default \code{TRUE}.}

\item{cv_scheme}{\code{NULL} (default) requires \code{suite} to already cover a
single CV scheme; pass a scheme name to filter to it explicitly. Errors
rather than silently pooling multiple schemes together.}
}
\value{
A \code{data.frame} in long form: \code{dataset}, \code{estimator}, \code{value}
(the aggregated metric), \code{relative} (ratio to the baseline, \code{NA} where
the baseline itself has no value for that dataset).
}
\description{
Aggregates each (dataset, estimator) pair's metric across CV schemes
(median, to avoid mixing schemes into a single number silently) then
expresses it relative to a baseline estimator's value on the \emph{same}
dataset. Values \verb{< 1} mean "better than baseline" regardless of the
metric's own direction -- a heatmap can use one consistent color scale.
}
