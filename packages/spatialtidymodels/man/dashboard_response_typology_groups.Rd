% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_response_typology_groups}
\alias{dashboard_response_typology_groups}
\title{Group datasets by response type for subgroup analysis}
\usage{
dashboard_response_typology_groups(dataset_metadata, min_datasets = 4L)
}
\arguments{
\item{dataset_metadata}{\code{suite$dataset_metadata} (or an equivalent
\code{data.frame} with \code{dataset} and \code{response_typology} columns).}

\item{min_datasets}{Minimum number of datasets with a known
\code{response_typology} required before grouping is attempted, same
never-fabricate-a-grouping rule as \code{\link[=dashboard_n_tertile_groups]{dashboard_n_tertile_groups()}}.
Default \code{4}.}
}
\value{
A \code{data.frame(dataset, response_typology)}, or \code{NULL} when the
metadata is absent, too small, or has only one response type present
(nothing to contrast).
}
\description{
Builds a \code{groups} data.frame usable directly as
\code{compare_estimator_variant(groups = ...)}'s subgroup dimension, from
\code{dataset_metadata$response_typology} (\code{"continuous"}/\code{"binary"}/\code{"count"}
-- see \code{\link[=detect_response_typology_from_spec]{detect_response_typology_from_spec()}},
\code{\link[=dashboard_suite_from_seed_runs]{dashboard_suite_from_seed_runs()}}). Mixing continuous and non-continuous
datasets under one RMSE/win-rate figure is not meaningful (a "win" on a
binary dataset's rmse/mae is on a probability scale, not the same thing as
a continuous dataset's rmse/mae) -- this dimension lets a comparison show
whether an advantage holds within each response type rather than only in
aggregate.
}
