% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_relative_metric_by_estimator}
\alias{dashboard_relative_metric_by_estimator}
\title{Median relative metric per estimator, aggregated across datasets}
\usage{
dashboard_relative_metric_by_estimator(
  suite,
  baseline_estimator,
  metric = "rmse",
  lower_is_better = TRUE,
  cv_scheme = NULL
)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} or a results-shaped \code{data.frame}.}

\item{baseline_estimator}{Estimator every other estimator is expressed
relative to, e.g. \code{"ols"}.}

\item{metric}{Metric to compute the ratio on. Default \code{"rmse"}.}

\item{lower_is_better}{Whether lower values of \code{metric} are better.
Default \code{TRUE}.}

\item{cv_scheme}{\code{NULL} (default) requires \code{suite} to already cover a
single CV scheme; pass a scheme name to filter to it explicitly. Errors
rather than silently pooling multiple schemes together.}
}
\value{
A \code{data.frame}, one row per estimator, ordered best-first:
\code{estimator}, \code{median_relative}, \code{n_datasets} (how many datasets
contributed a finite relative value).
}
\description{
Response variables are not on a comparable scale between datasets, so a
raw metric aggregated across datasets (e.g. \code{median(rmse)} mixing a
dataset where Y is in the 0-1 range with one where Y is in the millions)
is not a valid cross-dataset ranking -- see \code{\link[=dashboard_relative_metric]{dashboard_relative_metric()}}.
This is the function an Overview KPI card or a performance-vs-duration
scatter should use for a cross-dataset Y axis: the per-(dataset,
estimator) relative ratio from \code{dashboard_relative_metric()}, aggregated
to one value per estimator via the median across datasets (not the mean,
so one dataset with an extreme ratio can't dominate).
}
