% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_task_source_counts}
\alias{dashboard_task_source_counts}
\title{Count benchmark tasks vs. independent data sources in a suite}
\usage{
dashboard_task_source_counts(suite)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} (uses its \code{dataset_metadata}
directly -- see \code{\link[=benchmark_spatial_suite]{benchmark_spatial_suite()}} -- rather than rebuilding
it), or a character vector of dataset/benchmark-task names.}
}
\value{
A list: \code{n_benchmark_tasks}, \code{n_independent_sources}, \code{table} (a
\code{data.frame} with \code{dataset}, \code{source_dataset_id}, \code{benchmark_task_id}
for each dataset in \code{suite}; a dataset absent from the package metadata
registry -- e.g. a synthetic/ad hoc dataset used outside the registry --
is treated as its own task and its own source, the same fallback
convention as \code{metadata_dataset_registry()}).
}
\description{
A dataset split into several benchmark tasks from the same underlying data
collection (e.g. one country's housing data split into per-year files,
see \code{korea_hedonic_housing}) is one independent \emph{source}, not several --
see \verb{wiki/metadata/dataset_distribution_architecture_2026-08.md}. This
counts both so a dashboard can report "N benchmark tasks / N independent
sources" rather than implying every task is an independent source.
}
