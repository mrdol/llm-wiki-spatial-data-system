% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/71-benchmark-panel-spatial.R
\name{benchmark_spatial_panel}
\alias{benchmark_spatial_panel}
\title{Run spatial panel estimators on one dataset}
\usage{
benchmark_spatial_panel(
  formula,
  data,
  panel,
  W = NULL,
  estimators = c("panel_fe", "panel_sar_fe"),
  model_type = c("within", "random"),
  cv_scheme = c("panel_full_fit", "panel_time_holdout", "panel_rolling_origin"),
  h = NULL,
  initial = NULL,
  assess = NULL,
  skip = 0L
)
}
\arguments{
\item{formula}{A model formula.}

\item{data}{A data frame with one row per unit-time observation.}

\item{panel}{A \code{spatial_panel_spec} (see \code{\link[=spatial_panel_spec]{spatial_panel_spec()}}).}

\item{W}{Optional spatial weights matrix defined over units, required by
estimators with \code{requires_W = TRUE} in \code{\link[=available_panel_estimators]{available_panel_estimators()}}.}

\item{estimators}{Panel estimators to run: \code{"panel_fe"}, \code{"panel_sar_fe"},
\code{"panel_sem_fe"}, \code{"panel_sac_fe"}, \code{"panel_sdm_fe"} (see
\code{\link[=available_panel_estimators]{available_panel_estimators()}}). \code{panel_sdm_fe} requires a balanced
panel; SEM/SAC/SDM/SAR prediction requires every unit present in each
test period (see \code{\link[=predict_panel_fit]{predict_panel_fit()}}); all prediction targets require
\code{panel$effect == "individual"}.}

\item{model_type}{\code{"within"} (fixed effects) or \code{"random"}.}

\item{cv_scheme}{\code{"panel_full_fit"}, \code{"panel_time_holdout"}, or
\code{"panel_rolling_origin"}.}

\item{h}{Number of trailing periods held out, for \code{cv_scheme = "panel_time_holdout"} (see \code{\link[=panel_time_holdout]{panel_time_holdout()}}).}

\item{initial, assess, skip}{Rolling-origin window sizes, for \code{cv_scheme = "panel_rolling_origin"} (see \code{\link[=panel_rolling_origin]{panel_rolling_origin()}}).}
}
\value{
A \code{spatial_panel_benchmark} object with \code{results} (one row per
estimator for \code{panel_full_fit}; one row per fold/estimator/horizon for
the time-aware schemes) and \code{fits} (named list of \code{spatial_panel_fit}
objects -- for \code{panel_full_fit}, the single fit per estimator; for the
time-aware schemes, the fit from the LAST fold, kept for inspection).
}
\description{
Three mutually exclusive resampling schemes, chosen with \code{cv_scheme}:
\code{"panel_full_fit"} (replication: fit on the whole panel, no split),
\code{"panel_time_holdout"} (fit on all periods except the last \code{h}, predict
those \code{h}), and \code{"panel_rolling_origin"} (growing-window folds, see
\code{\link[=panel_rolling_origin]{panel_rolling_origin()}}). Cross-sectional resampling schemes
(\code{near_prediction}, \code{block_spatial}, \code{vfold_cv}) do not apply to unit-time
observations and are never offered here.
}
