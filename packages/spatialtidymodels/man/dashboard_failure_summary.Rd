% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_failure_summary}
\alias{dashboard_failure_summary}
\title{Summarize fit failures per estimator}
\usage{
dashboard_failure_summary(suite, cv_scheme = NULL)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} or a results-shaped \code{data.frame}.
Fold-level failure rate is used when \code{n_resamples}/\code{n_failed_resamples}
are present; otherwise falls back to counting rows with a non-\code{NA}
\code{fit_error}.}

\item{cv_scheme}{\code{NULL} (default) requires \code{suite} to already cover a
single CV scheme; pass a scheme name to filter to it explicitly. Errors
rather than silently pooling multiple schemes together.}
}
\value{
A \code{data.frame}, one row per estimator: \code{n_cases}, \code{n_failed}
(fold-level count when available, else case count), \code{failure_rate},
\code{datasets_affected}, \code{worst_case_failure_rate}. Estimators with zero
failures are still included (helps a dashboard show "0 failures"
explicitly rather than omitting a clean estimator).
}
\description{
Summarize fit failures per estimator
}
