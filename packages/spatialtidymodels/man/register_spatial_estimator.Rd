% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/17-estimator-registration.R
\name{register_spatial_estimator}
\alias{register_spatial_estimator}
\title{Register a custom spatial estimator for the benchmark}
\usage{
register_spatial_estimator(
  id,
  fit,
  predict,
  family = NA_character_,
  reference_estimator = NA_character_,
  variant_family = NA_character_,
  requires_coords = TRUE,
  requires_W = FALSE,
  tunable_parameters = character(0),
  package = NA_character_,
  notes = "",
  overwrite = FALSE
)
}
\arguments{
\item{id}{Estimator name, used in \code{estimators = c(...)} and in comparison
calls. Must not collide with a built-in estimator name.}

\item{fit}{A function with signature \verb{function(formula, data, coords)}
returning a fitted model object. If the estimator needs a spatial
weights matrix, build it inside \code{fit} (e.g. with \code{build_knn_W()}).}

\item{predict}{A function with signature \verb{function(fit, new_data)}
returning a numeric vector of predictions, same length and row order as
\code{new_data}.}

\item{family}{Free-text label grouping this estimator with related ones
(e.g. \code{"sar"}, \code{"boosting"}). Purely informative.}

\item{reference_estimator}{Name of the built-in (or another registered)
estimator this one should be compared against by default -- feeds
\code{compare_estimator_variant(reference = ...)} conventions and any future
dashboard grouping. Purely informative; not enforced. Supplying it also
sets \code{role = "variant"} in \code{available_benchmark_estimators()}/
\code{registered_spatial_estimators()}; leaving it \code{NA} gives \code{role = "reference"} (same two-role convention as the package's built-in
estimator taxonomy -- see \code{family}/\code{role}/\code{reference_estimator}/
\code{variant_family} columns there).}

\item{variant_family}{Free-text label for \emph{how} this estimator varies from
its reference (e.g. \code{"boosting"}, \code{"alternate_backend"}), mirroring the
built-in taxonomy's \code{variant_family} column. Purely informative.}

\item{requires_coords, requires_W}{Informative flags describing what the
estimator needs; \code{requires_W} does not yet trigger automatic \code{W}
construction (see Details above the source).}

\item{tunable_parameters}{Character vector naming hyperparameters, for
display in \code{available_benchmark_estimators()}. Tuning via
\code{benchmark_spatial(tune = TRUE)} is not yet wired for registered
estimators.}

\item{package}{Optional R package name this estimator depends on; used by
\code{available_benchmark_estimators(include_installed = TRUE)}. Leave \code{NA}
(default) if there is no package dependency to check.}

\item{notes}{Free-text notes shown in \code{available_benchmark_estimators()}.}

\item{overwrite}{If \code{FALSE} (default), re-registering an existing \code{id}
errors instead of silently replacing it.}
}
\value{
\code{id}, invisibly.
}
\description{
Plugs a user-supplied estimator into \code{benchmark_spatial()},
\code{available_benchmark_estimators()} and, downstream,
\code{compare_estimator_variant()}, without touching package internals. This is
the entry point for testing a new variant of a reference estimator (e.g. a
nonlinear or boosted SAR) against the package's built-in routes.
}
