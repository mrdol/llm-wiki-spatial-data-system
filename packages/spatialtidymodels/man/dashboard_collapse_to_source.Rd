% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_collapse_to_source}
\alias{dashboard_collapse_to_source}
\title{Collapse per-seed (or per-task) rows into one row per source dataset}
\usage{
dashboard_collapse_to_source(
  suite,
  dataset_metadata = NULL,
  fun = stats::median,
  metric = "rmse"
)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} (uses its \code{dataset_metadata} to
find \code{source_dataset_id}), or a results-shaped \code{data.frame} together
with an explicit \code{dataset_metadata}.}

\item{dataset_metadata}{Required when \code{suite} is a plain \code{data.frame};
ignored (the suite's own is used) when \code{suite} is a
\code{spatial_benchmark_suite}. Must have \code{dataset} and \code{source_dataset_id}.}

\item{fun}{Aggregation function applied to each metric column's finite
values within a group. Default \code{stats::median}.}

\item{metric}{Column whose task-to-task (seed-to-seed) spread is reported
in \code{dispersion_pct} -- see the return value. Default \code{"rmse"}; pass
whatever metric is actually being displayed (e.g. the Overview page's
selected metric) so the dispersion figure matches what the viewer is
looking at, not a different, silently-substituted one. Ignored (no
\code{dispersion_pct} column) if absent from \code{results} or not numeric.}
}
\value{
A \code{spatial_benchmark_suite}-shaped list (\code{results},
\code{dataset_metadata}, \code{datasets}, \code{estimators}, \code{cv_schemes}, \code{failures})
with one row per \verb{(source_dataset_id, cv_scheme, estimator)}; \code{dataset}
is renamed to the source id. Character diagnostic columns
(\code{fit_error}/\code{moran_error}/\code{spatial_param}) keep every distinct
non-\code{NA} value, \code{" | "}-joined, same convention as
\code{\link[=summarize_resample_results]{summarize_resample_results()}}. \code{n}/\code{n_resamples}/\code{n_failed_resamples}
are summed (they are counts, not ratios); \code{inla_attempts_max} takes the
max. \code{results$dispersion_pct} is \code{100 * range(metric) / median(metric)}
across the collapsed tasks/seeds -- \code{NA} for a group with only one task
(nothing to disperse) or a non-finite median. \code{results$n_tasks_collapsed}
records how many rows were merged into each output row (\code{1} when
nothing was actually collapsed).
}
\description{
A dataset split into several benchmark rows sharing the same
\code{source_dataset_id} -- most commonly one seed of a fold-split repeated
several times (see \code{\link[=dashboard_suite_from_seed_runs]{dashboard_suite_from_seed_runs()}}, \code{"<dataset>__s<seed>"}),
but the same shape applies to any other multi-task source (e.g. a country's
data split into yearly files) -- clutters an Overview built for "what
happened, at a glance": every KPI card, the heatmap and the failure table
would otherwise show \verb{N tasks} rows instead of \verb{N sources}, several of them
near-duplicates of each other. This collapses each \verb{(source_dataset_id, cv_scheme, estimator)} group into one row via \code{fun} (default \code{median},
consistent with every other cross-dataset summary in this file), the same
descriptive statistic \link[=compare_estimator_variant(analysis_unit =
"source")]{compare_estimator_variant(analysis_unit = "source")} uses to keep a verdict from being dominated by a source that
happens to have more tasks than the others.
}
\details{
Never used for the rigorous Comparison-tab statistics (the paired
Wilcoxon test there needs the underlying tasks/seeds as separate cases,
not one pre-averaged number per source -- see
\code{compare_estimator_variant()}'s own \code{analysis_unit = "source"} for that
collapse done the statistically correct way, downstream of the per-case
deltas). This is strictly a \emph{display} aggregation for a suite's
\code{results}/\code{dataset_metadata}.
}
