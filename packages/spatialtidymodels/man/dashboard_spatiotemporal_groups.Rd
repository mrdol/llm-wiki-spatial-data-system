% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_spatiotemporal_groups}
\alias{dashboard_spatiotemporal_groups}
\title{Group datasets by cross-sectional vs. spatio-temporal structure}
\usage{
dashboard_spatiotemporal_groups(dataset_metadata, min_datasets = 4L)
}
\arguments{
\item{dataset_metadata}{\code{suite$dataset_metadata} (or an equivalent
\code{data.frame} with \code{dataset} and \code{spatio_temporal} columns).}

\item{min_datasets}{Minimum number of datasets with a known
\code{spatio_temporal} flag required before grouping is attempted, same
never-fabricate-a-grouping rule as \code{\link[=dashboard_n_tertile_groups]{dashboard_n_tertile_groups()}}.
Default \code{4}.}
}
\value{
A \code{data.frame(dataset, spatio_temporal)} with \code{spatio_temporal}
recoded to \code{"spatio_temporal"}/\code{"cross_section"} (readable subgroup
labels), or \code{NULL} when the metadata is absent, too small, or has only
one kind of dataset present (nothing to contrast).
}
\description{
Builds a \code{groups} data.frame usable directly as
\code{compare_estimator_variant(groups = ...)}'s subgroup dimension, from
\code{dataset_metadata$spatio_temporal} (\code{TRUE} for a panel dataset with a time
dimension, e.g. eligible for \code{inla_spde_st} -- see
\code{\link[=dashboard_suite_from_seed_runs]{dashboard_suite_from_seed_runs()}}). A candidate estimator built for a
space x time field (or, symmetrically, one that ignores time) may behave
very differently on the two kinds of dataset; this dimension surfaces that
split instead of averaging it away.
}
