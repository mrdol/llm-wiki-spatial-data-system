% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/16-estimator-comparison.R
\name{comparison_rules}
\alias{comparison_rules}
\title{Define the decision thresholds used to turn a comparison into a verdict}
\usage{
comparison_rules(
  min_win_rate = 0.7,
  max_large_loss_rate = 0.1,
  large_loss_threshold = 0.1,
  min_median_delta = 0,
  max_failure_rate_increase = 0.05,
  rope = 0.01,
  alpha = 0.05,
  min_cases_for_verdict = 10L,
  secondary_guardrails = c(),
  max_runtime_multiplier = NA_real_,
  min_cases_for_subgroup = 5L,
  require_significance_for_subgroup = FALSE,
  analysis_unit = c("task", "source")
)
}
\arguments{
\item{min_win_rate}{Minimum share of non-tied, non-failed cases the
candidate must win for a \code{"SUPERIOR"} verdict (and, symmetrically, the
minimum loss share for \code{"INFERIOR"}). Default \code{0.70}.}

\item{max_large_loss_rate}{Maximum share of cases where the candidate is
allowed to degrade the primary metric by more than
\code{large_loss_threshold}. Default \code{0.10}.}

\item{large_loss_threshold}{Relative degradation (as a proportion, e.g.
\code{0.10} = 10\%) on the primary metric beyond which a single case counts as
a "large loss" for the candidate (or, symmetrically, a "large win" for
the candidate when used as the \code{"INFERIOR"} guardrail). Default \code{0.10}.}

\item{min_median_delta}{Minimum median relative improvement (in
percentage points, e.g. \code{0} requires the median case to not be worse) the
candidate must show for \code{"SUPERIOR"}. Default \code{0}.}

\item{max_failure_rate_increase}{Maximum tolerated increase in fit-failure
rate (candidate vs reference, as a proportion) before the verdict is
forced to \code{"UNSTABLE"} regardless of how the candidate scores otherwise
-- a variant that wins on RMSE but fails to fit far more often than the
reference is not a safe default. When fold-level \code{n_resamples}/
\code{n_failed_resamples} are available, the failure rate is computed per
case as \code{n_failed_resamples / n_resamples} (so a candidate failing 2 of
5 folds counts as 40\% failed for that case, even though it still
produced an aggregate RMSE from the other 3); otherwise it falls back to
an all-or-nothing flag (did this case produce a metric at all). Default
\code{0.05}.}

\item{rope}{Region of practical equivalence on the primary metric's
relative delta (as a proportion, e.g. \code{0.01} = 1\%). Cases inside
\verb{[-rope, +rope]} count as \code{"TIE"} rather than \code{"WIN"}/\code{"LOSS"}, so a
\verb{+0.001\%} improvement isn't counted as a win. It is also the criterion
for a genuine \code{"EQUIVALENT"} verdict (median delta inside the ROPE),
as opposed to \code{"INCONCLUSIVE"} (see \code{\link[=compare_estimator_variant]{compare_estimator_variant()}}).
Default \code{0.01}.}

\item{alpha}{Significance level for the paired Wilcoxon signed-rank test on
the primary metric's per-case delta. Default \code{0.05}.}

\item{min_cases_for_verdict}{Minimum number of valid (non-failed) cases
required before issuing any verdict other than
\code{"INSUFFICIENT_EVIDENCE"}. Default \code{10}.}

\item{secondary_guardrails}{Named numeric vector of maximum tolerated
median relative degradation (as a proportion) on secondary metrics,
e.g. \code{c(mae = 0.03, moran_abs = 0.05)}. When the primary metric would
otherwise earn a \code{"SUPERIOR"} verdict but the candidate's median case
degrades a guarded metric beyond its threshold, the verdict becomes
\code{"TRADEOFF"} instead. Empty by default (no secondary guardrail active)
so existing calls keep their current behaviour unless you opt in.}

\item{max_runtime_multiplier}{Maximum tolerated ratio of candidate to
reference \code{duration_sec} (median across cases) before it counts as a
\code{"TRADEOFF"} guardrail breach, e.g. \code{5} = candidate may be up to 5x
slower. \code{NA} (default) disables the check.}

\item{min_cases_for_subgroup}{Minimum number of cases a subgroup (see the
\code{groups} argument of \code{\link[=compare_estimator_variant]{compare_estimator_variant()}}) must have before it
can trigger a \code{"SPECIALIZED"} verdict. Deliberately smaller than
\code{min_cases_for_verdict} -- subgroups are inherently smaller slices of the
same evidence -- but still a real floor against calling 2 lucky datasets
a "systematic advantage". Default \code{5}.}

\item{require_significance_for_subgroup}{If \code{TRUE}, a subgroup must also
clear its own paired Wilcoxon test (at \code{alpha}) to count towards
\code{"SPECIALIZED"}. Default \code{FALSE}: with \code{min_cases_for_subgroup} this
small, few subgroups will ever reach significance, so \code{"SPECIALIZED"} is
treated as an exploratory/hypothesis-generating signal by default, not a
confirmatory one -- documented as such rather than silently unreachable.}

\item{analysis_unit}{\code{"task"} (default) treats every \verb{(dataset, cv_scheme)}
case as independent evidence, exactly the current/historical behaviour.
\code{"source"} first collapses every case sharing the same
\code{source_dataset_id} (see \code{dashboard_task_source_counts()},
\verb{wiki/metadata/dataset_distribution_architecture_2026-08.md}) into one
representative case per source, per \code{cv_scheme} -- the median of the
per-task relative deltas (primary metric and every secondary/guardrail
metric) across that source's tasks -- before win/tie/loss and the
Wilcoxon test run. This prevents a dataset split into many benchmark
tasks (e.g. \code{korea_hedonic_housing}'s 32 yearly splits) from outweighing
a source evaluated as a single task just by volume. A source counts as
failed on a side (reference/candidate) only if \emph{every} one of its tasks
failed on that side; failure rates are collapsed the same way (median
across the source's tasks). Requires a dataset -> \code{source_dataset_id}
mapping: automatic when \code{suite} is a \code{spatial_benchmark_suite} (via
\verb{$dataset_metadata}), otherwise pass \code{dataset_metadata} explicitly to
\code{\link[=compare_estimator_variant]{compare_estimator_variant()}}. \code{groups} (subgroup analysis) is not
supported yet under \code{analysis_unit = "source"} -- mixing a source-level
verdict with task-level subgroups would silently blend two different
units of evidence, so it errors instead. Changes which cases feed the
verdict relative to \code{"task"}, i.e. can change verdicts on existing
benchmarks -- use deliberately, not as a silent default.}
}
\value{
A \code{spatial_comparison_rules} object, a plain list of thresholds.
}
\description{
These thresholds are project conventions, not universal statistical
constants -- adjust them to whatever your benchmark protocol actually
requires and document the choice alongside your results.
}
