% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/51-parsnip-inlaspde.R
\name{inla_spde_reg}
\alias{inla_spde_reg}
\title{Specification parsnip pour un champ spatial INLA SPDE}
\usage{
inla_spde_reg(
  mode = "regression",
  coords = NULL,
  family = "gaussian",
  link = NULL,
  prior_range = NULL,
  prior_sigma = NULL,
  mesh_max_edge = NULL,
  mesh_cutoff = NULL,
  time = NULL
)
}
\arguments{
\item{mode}{Mode parsnip. Seul \code{"regression"} est supporte -- y compris
pour \code{family = "binomial"}, ou la prediction retournee est la
probabilite continue (meme convention que \code{ols}/\code{gam_spatial} pour les
taches binaires dans ce package: pas de mode \code{"classification"} distinct).}

\item{coords}{Colonnes de coordonnees disponibles dans le workflow.}

\item{family}{Famille \code{inlabru}/\code{INLA}: \code{"gaussian"} (defaut), \code{"binomial"}
ou \code{"poisson"}.}

\item{link}{Lien explicite, ou \code{NULL} (defaut = lien canonique de la
famille: identite/logit/log). Seul \code{"cloglog"} est accepte pour
\code{family = "binomial"} (verifie empiriquement contre \code{inlabru::bru()},
motive par \code{paper_flapper_skate_presence}, Loca et al. 2025, qui utilise
ce lien) ; aucun lien alternatif n'est supporte pour \code{"gaussian"}/
\code{"poisson"} dans cette phase.}

\item{prior_range}{PC-prior \code{c(range0, p)} pour \code{INLA::inla.spde2.pcmatern()}.
\code{NULL} (defaut): calcule depuis la diagonale de la bounding box des
coordonnees, voir \code{inla_spde_default_priors()}.}

\item{prior_sigma}{PC-prior \code{c(sigma0, p)} pour \code{INLA::inla.spde2.pcmatern()}.
\code{NULL} (defaut): calcule depuis l'ecart-type empirique de la reponse pour
\code{family = "gaussian"} (echelle reponse = echelle du champ, lien
identite), ou fixe a 1 pour les familles a lien non-identite (echelle du
predicteur lineaire, pas celle de la reponse brute -- valeur alignee sur
le prior faiblement informatif utilise par Loca et al. 2025, ecart-type
~1.4 teste jusqu'a 0.5). Voir \code{inla_spde_default_priors()}.}

\item{mesh_max_edge}{\code{max.edge} de \code{fmesher::fm_mesh_2d_inla()}. \code{NULL}
(defaut): calcule depuis la diagonale de la bounding box, voir
\code{inla_spde_default_mesh()}.}

\item{mesh_cutoff}{\code{cutoff} de \code{fmesher::fm_mesh_2d_inla()}. \code{NULL}
(defaut): calcule depuis la diagonale de la bounding box.}

\item{time}{Nom de colonne (chaine) a utiliser comme index de groupe
temporel pour un champ spatio-temporel separable espace x AR1 (variante
\code{inla_spde_st}). \code{NULL} (defaut): champ purement spatial, cross-section,
comportement inchange. Voir \code{inlaspde_fit_impl()}.}
}
\value{
Une specification de modele \code{parsnip}.
}
\description{
Cree une specification \code{parsnip} pour une regression avec un champ spatial
gaussien de Matern (SPDE, PC-priors), ajustee par \code{inlabru::bru()}.
}
\section{Reproductibilite}{

Par defaut la \strong{prediction est deterministe} (1000 echantillons a
posteriori, graine fixe cote INLA et cote R, sans modifier le generateur
aleatoire de R de l'appelant) et le fit et la prediction s'executent sous
\code{num.threads = "1:1"} (option globale d'INLA restauree ensuite), car le
multi-thread par defaut d'INLA donne des hyperparametres qui varient d'un
appel a l'autre et divergent parfois. Le \strong{fit reste} toutefois
legerement variable en mode INLA \code{compact} (defaut) : la seule
configuration mesuree comme exactement deterministe est
\code{options(spatialtidymodels.inla_mode = "classic")}, mais elle est beaucoup
plus lente sur certains modeles (ex. 704 s contre 17 s pour un
\code{inla_spde_st} binomial) et peut converger ailleurs -- elle n'est donc pas
activee par defaut. Autres options : \code{spatialtidymodels.inla_reproducible}
(\code{TRUE} ; \code{FALSE} restaure le comportement natif d'INLA/inlabru),
\code{spatialtidymodels.inla_pred_samples} (\code{1000L}) et
\code{spatialtidymodels.inla_pred_seed} (\code{1L}, non nul). La graine fixe rend la
prediction reproductible, mais l'erreur Monte-Carlo de l'estimation de la
moyenne a posteriori subsiste (elle diminue avec le nombre d'echantillons).
}

