% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/19-dashboard-app.R
\name{launch_benchmark_dashboard}
\alias{launch_benchmark_dashboard}
\title{Launch the benchmark dashboard}
\usage{
launch_benchmark_dashboard(suite, baseline_estimator = "ols", launch = TRUE)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} (from \code{\link[=benchmark_spatial_suite]{benchmark_spatial_suite()}})
or a results-shaped \code{data.frame}.}

\item{baseline_estimator}{Estimator every relative KPI/heatmap/scatter is
expressed against by default. Must be present in \code{suite}'s \code{estimator}
column. Default \code{"ols"}.}

\item{launch}{If \code{TRUE} (default), blocks and runs the app
(\code{shiny::runApp()}). If \code{FALSE}, returns the \code{shiny::shinyApp()} object
without running it -- used by tests and for embedding in another app.}
}
\value{
Invisibly, the result of \code{shiny::runApp()} when \code{launch = TRUE};
otherwise the \code{shiny.appobj} itself.
}
\description{
Opens a multi-page Shiny app for a benchmark suite: an Overview (headline
KPIs, relative-metric heatmap, performance-vs-duration, residual spatial
dependence, fold failures), plus Reference vs Variant / Datasets / CV
Schemes / Methodology pages, navigated from a single left sidebar. Filters
only subset already-computed results -- nothing is refit when a filter or
page changes.
}
