% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/40-parsnip-spmoran.R
\name{spmoran_esf_with_vif_fallback}
\alias{spmoran_esf_with_vif_fallback}
\title{Work around a spmoran::esf() bug on very sparse spatial eigenstructures}
\usage{
spmoran_esf_with_vif_fallback(y, x, meig, vif)
}
\description{
spmoran::esf()'s default stepwise eigenvector selection (\code{fn = "r2"})
removes each candidate eigenvector from its pool as it's evaluated at
every step (\code{sf <- sf[, -Obj_ind]}), regardless of \code{vif} -- \code{vif} only
gates whether a candidate is \emph{accepted} into the running model at each
step, not whether the depleting search itself runs (confirmed by reading
esf.R: the \code{while} loop that does this is guarded only by \code{fn != "all"}).
When there are very few candidate eigenvectors to begin with (confirmed
down to 2 on a dataset with tightly clustered coordinates,
paper_snake_home_range) and the stepwise search ends up exhausting all of
them, esf()'s own "all selected" branch (esf.R, \verb{if (i == ne)}) rebuilds
its output table from that now-emptied pool instead of the eigenvectors
actually chosen, ending up short a column and crashing with "'names'
attribute \link{10} must be the same length as the vector \link{9}" (exact numbers
vary with \code{ne}/covariate count). Root-caused 2026-09-13 by reproducing the
failing near_prediction folds directly and walking the call stack into
spmoran's own source -- confirmed entirely inside spmoran::esf(), not in
how we call it. \code{vif = NULL} alone does NOT avoid this (verified
empirically: same crash) -- only \code{fn = "all"} does, since it skips the
stepwise \code{while} loop entirely and keeps every eigenvector directly.
}
\keyword{internal}
