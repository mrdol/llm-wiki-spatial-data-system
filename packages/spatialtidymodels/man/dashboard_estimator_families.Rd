% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_estimator_families}
\alias{dashboard_estimator_families}
\title{Join estimators present in a suite with their taxonomy family and dashboard group}
\usage{
dashboard_estimator_families(suite)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} or a results-shaped \code{data.frame}.}
}
\value{
A \code{data.frame} with \code{estimator}, \code{family}, \code{dashboard_group}
(both fall back to \code{"other"}/\code{"Other"} when the estimator has no
taxonomy entry, e.g. a custom estimator registered without one).
}
\description{
Thin wrapper around \code{\link[=available_benchmark_estimators]{available_benchmark_estimators()}} restricted to the
estimators actually present in \code{suite}'s results -- used to color a
performance/duration scatter by family, and to drive a dashboard_group
sidebar nav.
}
\details{
\code{family} (the scientific model family, e.g. \code{"SAR"}) and \code{dashboard_group}
(which dashboard menu section to list the estimator under, e.g.
\code{"Boosting"}) are deliberately different columns and can disagree: an
estimator like \code{spboost_bspa_sar_ml} is scientifically a SAR (\code{family = "SAR"}) but implemented via a boosting backend, so it belongs under a
\code{"Boosting"} dashboard section -- never conflate the two.
}
