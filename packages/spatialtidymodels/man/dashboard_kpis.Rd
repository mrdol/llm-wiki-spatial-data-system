% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/18-dashboard-data.R
\name{dashboard_kpis}
\alias{dashboard_kpis}
\title{Compute headline KPIs for a benchmark suite}
\usage{
dashboard_kpis(
  suite,
  metrics = c("rmse", "mae", "moran_abs", "duration_sec"),
  lower_is_better = c(rmse = TRUE, mae = TRUE, moran_abs = TRUE, duration_sec = TRUE),
  cv_scheme = NULL
)
}
\arguments{
\item{suite}{A \code{spatial_benchmark_suite} or a results-shaped \code{data.frame}.}

\item{metrics}{Metrics to report a champion for, in order.}

\item{lower_is_better}{Named logical vector giving each metric's
direction. Metrics not listed default to \code{TRUE}.}

\item{cv_scheme}{\code{NULL} (default) requires \code{suite} to already cover a
single CV scheme; pass a scheme name to filter to it explicitly. Errors
rather than silently pooling multiple schemes together -- see
\code{\link[=dashboard_require_single_cv_scheme]{dashboard_require_single_cv_scheme()}}.}
}
\value{
A \code{data.frame}, one row per metric, with \code{metric}, \code{value},
\code{estimator}, \code{dataset}, \code{cv_scheme}. A metric with no finite value
anywhere gets an \code{NA} row rather than being dropped, so a caller can
still render an (empty) card for it.
}
\description{
For each requested metric, finds the single best (dataset, estimator,
cv_scheme) row -- the "champion" -- among cases with no fit error. Meant
for the four value-box style cards on a dashboard Overview page.
}
