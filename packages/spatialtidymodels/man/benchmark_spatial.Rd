% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13-benchmark-spatial.R
\name{benchmark_spatial}
\alias{benchmark_spatial}
\title{Run an automatic spatial benchmark}
\usage{
benchmark_spatial(
  formula,
  data,
  coords,
  estimators = c("ols", "gam_spatial", "sar_lag", "sem_error", "sdm_mixed"),
  k_neighbors = 8,
  style = "W",
  zero_policy = TRUE,
  W = NULL,
  spboost_mstop = 100L,
  spboost_nu = 0.1,
  gamboost_mstop = 100L,
  gamboost_nu = 0.1,
  mgwrsar_bandwidth = 20,
  mgwrsar_kernel = "gauss",
  mgwrsar_fixed_vars = NULL,
  spmoran_enum = NULL,
  spmoran_vif = 10,
  tune = FALSE,
  resamples = NULL,
  tuning_grids = NULL,
  tuning_folds = 3L,
  cv_scheme = c("in_sample", "holdout_10pct", "near_prediction", "block_spatial",
    "vfold_cv", "custom"),
  eval_resamples = NULL,
  eval_folds = 5L,
  holdout_prop = 0.9,
  near_n_reps = 3L,
  near_test_size = NULL,
  block_folds = 5L,
  seed = 123L,
  verbose = FALSE,
  parallel = FALSE,
  workers = max(1L, parallel::detectCores(logical = FALSE) - 1L),
  allow_heavy_tuning = FALSE,
  fold_timeout_sec = NA_real_
)
}
\arguments{
\item{formula}{Common model formula.}

\item{data}{Data frame.}

\item{coords}{Coordinate column names.}

\item{estimators}{Estimators to run.}

\item{k_neighbors}{Number of nearest neighbours used to build kNN weights.}

\item{style}{Row-standardization style passed to \code{spdep::nb2listw()}.}

\item{zero_policy}{Zero-neighbour policy passed to \code{spdep}.}

\item{W}{Optional spatial weights matrix or \code{listw} object. When supplied,
SAR/SEM/SDM routes and residual Moran diagnostics use it instead of
rebuilding a kNN structure from \code{coords}.}

\item{spboost_mstop}{Number of boosting iterations for \code{spboost}.}

\item{spboost_nu}{Learning rate for \code{spboost}.}

\item{gamboost_mstop}{Number of boosting iterations for \code{mboost::gamboost}.}

\item{gamboost_nu}{Learning rate for \code{mboost::gamboost}.}

\item{mgwrsar_bandwidth}{Spatial bandwidth \code{H} for \code{mgwrsar} variants.}

\item{mgwrsar_kernel}{Spatial kernel for \code{mgwrsar} variants.}

\item{mgwrsar_fixed_vars}{Character vector of stationary coefficients for
mixed MGWRSAR models.}

\item{spmoran_enum}{Optional number of Moran eigenvectors for SpMoran ESF
and RE-ESF.}

\item{spmoran_vif}{VIF threshold for SpMoran ESF.}

\item{tune}{If \code{TRUE}, run \code{tune::tune_grid()} before the final fit for
estimators with a supported tuning route.}

\item{resamples}{\code{rsample} object used for tuning. If \code{NULL} and
\code{tune = TRUE}, a classic \code{vfold_cv()} is created.}

\item{tuning_grids}{Optional named list of tuning grids, one per estimator.}

\item{tuning_folds}{Number of folds for the default tuning \code{vfold_cv()}.}

\item{cv_scheme}{Evaluation scheme. \code{"in_sample"} keeps the diagnostic on
the full data; \code{"holdout_10pct"}, \code{"near_prediction"}, \code{"block_spatial"}
and \code{"vfold_cv"} score models out-of-sample; \code{"custom"} uses
\code{eval_resamples}.}

\item{eval_resamples}{Custom \code{rsample} object for out-of-sample evaluation.}

\item{eval_folds}{Number of folds for \code{cv_scheme = "vfold_cv"}.}

\item{holdout_prop}{Proportion kept in the training set for
\code{cv_scheme = "holdout_10pct"}.}

\item{near_n_reps}{Number of near-prediction repetitions.}

\item{near_test_size}{Target number of near-prediction test cells.}

\item{block_folds}{Number of spatial block folds.}

\item{seed}{Random seed used for resampling.}

\item{verbose}{If \code{TRUE}, print progress messages during tuning,
resampling evaluation, and final fits.}

\item{parallel}{If \code{TRUE}, evaluate manual MGWRSAR tuning candidates in
parallel with \code{parallel::makeCluster()}.}

\item{workers}{Number of parallel workers when \code{parallel = TRUE}.}

\item{allow_heavy_tuning}{If \code{FALSE}, protect large datasets from tuning
several expensive MGWRSAR estimators in the same call.}

\item{fold_timeout_sec}{Maximum wall-clock seconds allowed for a single
(estimator, fold) case in the cross-validated evaluation path (\code{fit},
\code{predict} and diagnostics combined) -- see \code{\link[=evaluate_benchmark_resamples]{evaluate_benchmark_resamples()}}/
\code{run_with_fold_timeout()} (\code{26-fold-timeout-worker.R}). \code{NA} (default)
disables the guard entirely, identical to before this argument existed
-- zero overhead, no subprocess ever spawned. When set, a case that
exceeds the budget is aborted and recorded as a normal failure with
\code{fit_error} starting \code{"TIMEOUT:"} instead of freezing the whole suite;
every other case keeps running unaffected. Enforced by running the fold
in a persistent \code{callr} worker process and killing it on timeout -- this
is a hard, OS-level guarantee, unlike base R's \code{setTimeLimit()}, which
was tested against the real pathology this guard was built for
(\code{stats::AIC()} on an \code{mboost}-derived engine) and never interrupted it,
even 40s+ past a 3s budget: R only checks for interrupts between
evaluator statements, never mid-way through one long-running
compiled/matrix call, which is exactly what that case was doing. This is
a generic safety net for unforeseen pathological cases on large
datasets, not a diagnosis of \emph{why} a case is slow -- a confirmed root
cause should still be fixed directly when found (the \code{stats::AIC()} case
above already was, in \code{extract_information_criteria()},
\code{12-diagnose-spatial.R}). Not yet applied to the final full-data
fit/diagnostics pass or to \code{cv_scheme = "in_sample"}, since those need
the fitted model object to survive in this process for \code{bench$fits},
and some backends (e.g. xgboost) do not serialize reliably across a
process boundary.}
}
\value{
A \code{spatial_benchmark} object with \code{results}, \code{resample_results}, and
final \code{fits}.
}
\description{
Fits the requested estimators on one dataset and returns comparable
diagnostics. By default, results are in-sample diagnostics. Use \code{cv_scheme}
to evaluate out-of-sample performance with a holdout, near-prediction folds,
spatial blocks, classic v-fold cross-validation, or custom rsample objects.
}
\details{
Spatial machine-learning forest routes are available when their optional
packages are installed. \code{spatialml_grf} calls \code{SpatialML::grf()}, a
Geographical Random Forest that fits one local random forest per training
observation and predicts new observations from the nearest local forest.
\code{spatialrf} calls \code{spatialRF::rf_spatial()}, which augments a random forest
with spatial predictors such as Moran eigenvector maps; its most robust
diagnostic path is currently in-sample because the upstream package exposes
\code{get_predictions()} for fitted predictions. \code{rfgls} calls
\code{RandomForestsGLS::RFGLS_estimate_spatial()}, which combines random forests
with a GLS correction for spatial dependence using a nearest-neighbour
Gaussian-process/Vecchia-style approximation.

\code{AICc} and \code{logLik} are reported when the fitted backend exposes a
likelihood and a usable parameter count. For \code{spboost}, the package first
tries the standard R methods, then falls back to backend fields such as
\code{logl}/backend information and an explicit active-coefficient count when
available.

The \code{spatial_param} and \code{spatial_value} columns report a single explicit
spatial dependence parameter when the fitted backend exposes one. SAR-style
models usually report \code{rho}, the coefficient of the spatially lagged
response \verb{W y}. SEM-style models report \code{lambda}, the coefficient of the
spatially autocorrelated error process. Some MGWRSAR variants estimate a
local spatial parameter; those are summarized as \code{lambda_local_mean}.

Models such as \code{random_forest}, \code{spatialml_grf}, and \code{spatialrf} may use
coordinates, local forests, distances, or spatial predictors, but they do not
estimate one scalar econometric parameter equivalent to \code{rho} or \code{lambda}.
Their \code{spatial_param} and \code{spatial_value} columns therefore remain \code{NA}.

The \code{moran_i} column reports Moran's I on residuals, while \code{moran_abs}
reports \code{abs(moran_i)}. When the goal is to remove residual spatial
autocorrelation, \code{moran_abs} is usually the metric to minimize because both
positive clustering and negative checkerboard-like autocorrelation indicate
remaining spatial structure.

Runtime is recorded in seconds. The main \code{results} table reports
\code{duration_sec}, the total measured time spent by each estimator across all
evaluation folds. The fold-level \code{resample_results} table reports
\code{elapsed_sec}, the elapsed time for each estimator/fold pair. In in-sample
evaluations, \code{duration_sec} reports the elapsed time for the fit plus
diagnostic pass on the full data.
}
