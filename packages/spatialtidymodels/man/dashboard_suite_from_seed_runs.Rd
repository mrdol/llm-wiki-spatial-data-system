% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/26-dashboard-seed-bridge.R
\name{dashboard_suite_from_seed_runs}
\alias{dashboard_suite_from_seed_runs}
\title{Reconstruct a \code{spatial_benchmark_suite} from \code{tools/bench_inla_multiseed.R} output}
\usage{
dashboard_suite_from_seed_runs(dir, cv_scheme_label = "block_spatial")
}
\arguments{
\item{dir}{Directory containing the \verb{_folds.csv}/\verb{_preds.csv} files (e.g.
\code{"data/manifests/runs/inla_multiseed_2026-09-22"}).}

\item{cv_scheme_label}{Fixed \code{cv_scheme} value assigned to every row --
describes the actual validation scheme used (5-fold spatial-block CV,
k-means blocks assigned to folds), not the seed. Default
\code{"block_spatial"}, the existing convention's name for this scheme (see
\code{\link[=make_spatial_resamples]{make_spatial_resamples()}}).}
}
\value{
A \code{spatial_benchmark_suite} object (same class/shape as
\code{\link[=benchmark_spatial_suite]{benchmark_spatial_suite()}}'s return value): \code{results},
\code{dataset_metadata}, \code{datasets}, \code{estimators}, \code{cv_schemes}, \code{failures},
\code{protocol}.
}
\description{
Reads every \verb{<dataset>_s<seed>_folds.csv} / \verb{_preds.csv} pair in \code{dir},
recomputes per-estimator metrics from the pooled fold predictions (same
convention as \code{\link[=summarize_resample_results]{summarize_resample_results()}}: RMSE/MAE/etc. computed once
on every test prediction concatenated across folds, not averaged per-fold
first), and assembles a \code{spatial_benchmark_suite} the dashboard can consume
directly via \code{\link[=launch_benchmark_dashboard]{launch_benchmark_dashboard()}}.
}
\details{
Each (dataset, seed) becomes its own \code{dataset} row, named
\code{"<dataset>__s<seed>"} -- see the file header for why a seed cannot share a
\code{cv_scheme} with another seed of the same dataset. \code{dataset_metadata}
carries \code{source_dataset_id} (the real dataset name, shared across its
seeds), \code{seed}, \code{response_typology} and \code{spatio_temporal} (\code{TRUE} when the
run declared a time column, i.e. \code{inla_spde_st} was eligible) for the
dashboard's filters and subgroup analysis.

Residual spatial autocorrelation (\code{moran_i}/\code{moran_abs}) was never computed
by this ad hoc benchmark (only \code{truth}/\code{pred}/timing/INLA diagnostics were
recorded per fold) -- both columns come back all-\code{NA}, same handling as any
other suite missing them (guardrails and the Overview/Comparison plots that
depend on them degrade gracefully, never error).
}
